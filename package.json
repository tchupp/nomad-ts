{
  "name": "@nomad-ts/root",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples"
  ],
  "scripts": {
    "clean": "lerna run clean",
    "lint": "lerna run lint",
    "build": "lerna run build",
    "test": "lerna run test",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "fp-ts": "^2.8.1",
    "glob": "^7.1.6",
    "lerna": "^3.20.2"
  }
}
